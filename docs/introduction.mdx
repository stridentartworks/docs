---
title: "Welcome"
sidebarTitle: "Introduction"
description: "Get started with Pets Galore"
---

Power your product with real-time AI video. Build, connect, and run with your universal runtime.

## What is Pets Galore?

As an inference cloud platform, Pets Galore provides the infrastructure to stream AI video as it's generated.
Built for products that run video continuously, from interactive scenes to live worlds.

### Continuous streaming with real-time input
Create a session, stream output, and control it live.

<video autoPlay muted="muted" loop playsInline preload="auto" width="640" height="360" 
className="w-full aspect-video rounded-xl" src="https://framerusercontent.com/assets/rHX16wmPIvAKNf4AKz5oB7lzPg.mp4">
</video>

```typescript
import { createApp, createStore, videoStream } from '@urun/core';
const store = createStore({ endpoint: 'wss://your-app.urun.sh' });
const app = createApp({ store });
// Create the session
const session = await app.call('generate_video', {
  prompt: 'A live lofi room with a character reacting to music',
});
// Wire video stream into a <video> element
const video = session.stream('video');
document.querySelector('video').srcObject = videoStream(video);
// Send controls back
session.emit('control', { action: 'pause' });
```

## Get started

<Card title="Quickstart" icon="rocket" href="/docs/quickstart">
  Generate your first video in minutes
</Card>
<Card title="Features" icon="puzzle-piece" href="/docs/features">
  Discover building blocks for AI video production
</Card>

